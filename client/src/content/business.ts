// A1 Marine Storage — business contact details.
// Canonical data now lives in `@shared/business` (single source shared with the
// server for JSON-LD). Re-exported here so existing `@/content/business` imports
// keep working unchanged.
export { BUSINESS, SERVICE_AREA } from "@shared/business";
